# Log1p Exponential

Computes the natural logarithm of 1 plus the exponential of the input,
to handle large inputs.

## Usage

``` r
log1pexp(x)
```

## Arguments

- x:

  (vector): A numeric vector of inputs.

## Value

(vector): A numeric vector where each element is the result of log(1 +
exp(x)).

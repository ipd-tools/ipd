% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppboot_logistic.R
\name{efron_boot_logistic}
\alias{efron_boot_logistic}
\title{Efron bootstrap PPI for binary outcomes}
\usage{
efron_boot_logistic(
  X_l,
  Y_l,
  f_l,
  X_u,
  f_u,
  nboot = 1000,
  calibration_degree = 1,
  conditional = TRUE,
  statistic = NULL,
  seed = NULL,
  return_boot = FALSE,
  ...
)
}
\arguments{
\item{X_l}{Numeric design matrix for labeled observations. Rows must
correspond to \code{Y_l} and \code{f_l}.}

\item{Y_l}{Numeric vector or one-column matrix of observed binary outcomes
for labeled observations. Outcomes must be coded as 0 and 1.}

\item{f_l}{Numeric vector or one-column matrix of predicted probabilities
for labeled observations.}

\item{X_u}{Numeric design matrix for unlabeled observations. Rows must
correspond to \code{f_u}.}

\item{f_u}{Numeric vector or one-column matrix of predicted probabilities
for unlabeled observations.}

\item{nboot}{Integer number of bootstrap replicates. Default is 1000.}

\item{calibration_degree}{Integer degree of the raw polynomial in
\code{logit(f)} used by the calibration model. Default is 1.}

\item{conditional}{Logical. If \code{TRUE}, use conditional parametric resampling
from the calibrated probabilities. If \code{FALSE}, resample labeled outcomes
nonparametrically in the first bootstrap level.}

\item{statistic}{Optional function of the form \verb{function(X, y)} returning a
numeric vector. Defaults to logistic regression coefficients.}

\item{seed}{Optional integer random seed.}

\item{return_boot}{Logical. If \code{TRUE}, return bootstrap replicate matrices.}

\item{...}{Additional arguments accepted for compatibility with the \code{ipd()}
wrapper. They are currently unused.}
}
\value{
A list with the following components:
\describe{
\item{est}{Named numeric vector of bootstrap estimates computed on
generated unlabeled outcomes.}
\item{se}{Named numeric vector of bootstrap standard errors computed from
generated unlabeled outcomes.}
\item{labeled_est}{Named numeric vector of bootstrap estimates computed on
generated labeled outcomes.}
\item{labeled_se}{Named numeric vector of bootstrap standard errors
computed from generated labeled outcomes.}
\item{classical_est}{Named numeric vector of the downstream statistic
computed from the observed labeled data.}
\item{calibration_fit}{The initial binomial calibration fit returned by
\code{stats::glm.fit()}.}
\item{calibration_degree}{Integer calibration polynomial degree.}
\item{conditional}{Logical value indicating whether conditional resampling
was used.}
\item{nboot}{Integer number of bootstrap replicates.}
\item{boot_unlabeled}{Bootstrap replicate matrix for unlabeled outcomes
when \code{return_boot = TRUE}; otherwise \code{NULL}.}
\item{boot_labeled}{Bootstrap replicate matrix for labeled outcomes when
\code{return_boot = TRUE}; otherwise \code{NULL}.}
}
}
\description{
\code{efron_boot_logistic()} implements the PPboot1 algorithm from Efron (2025)
for binary responses. The prediction rule is calibrated on the labeled data,
bootstrap outcomes are generated from calibrated probabilities, and a
downstream logistic regression statistic is evaluated on each bootstrap
sample.
}
\details{
The calibration model is a binomial GLM using a raw polynomial in
\code{logit(f_l)} as the calibration design. The default
\code{calibration_degree = 1} matches the main PPboot1 calibration model. The
default downstream statistic is the coefficient vector from
\code{stats::glm.fit(x = X, y = y, family = stats::binomial())}.

Predicted probabilities in \code{f_l} and \code{f_u} are clipped internally to avoid
infinite logits.
}
\examples{
set.seed(1)
n_l <- 80
n_u <- 120
x_l <- rnorm(n_l)
x_u <- rnorm(n_u)

X_l <- cbind("(Intercept)" = 1, X1 = x_l)
X_u <- cbind("(Intercept)" = 1, X1 = x_u)
f_l <- plogis(-0.1 + 0.9 * x_l + rnorm(n_l, sd = 0.3))
f_u <- plogis(-0.1 + 0.9 * x_u + rnorm(n_u, sd = 0.3))
Y_l <- rbinom(n_l, size = 1, prob = plogis(-0.3 + 1.1 * x_l))

fit <- efron_boot_logistic(
  X_l = X_l, Y_l = Y_l, f_l = f_l,
  X_u = X_u, f_u = f_u,
  nboot = 25, seed = 123
)

fit$est
fit$se

}

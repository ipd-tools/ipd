% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppboot_ols.R
\name{efron_boot_ols}
\alias{efron_boot_ols}
\title{Efron bootstrap PPI for continuous outcomes}
\usage{
efron_boot_ols(
  X_l,
  Y_l,
  f_l,
  X_u,
  f_u,
  nboot = 1000,
  degree = NULL,
  max_degree = 5,
  conditional = TRUE,
  statistic = NULL,
  seed = NULL,
  return_boot = FALSE,
  ...
)
}
\arguments{
\item{X_l}{Numeric design matrix for labeled observations. Rows must
correspond to \code{Y_l} and \code{f_l}.}

\item{Y_l}{Numeric vector or one-column matrix of observed continuous
outcomes for labeled observations.}

\item{f_l}{Numeric vector or one-column matrix of predicted outcomes for
labeled observations.}

\item{X_u}{Numeric design matrix for unlabeled observations. Rows must
correspond to \code{f_u}.}

\item{f_u}{Numeric vector or one-column matrix of predicted outcomes for
unlabeled observations.}

\item{nboot}{Integer number of bootstrap replicates. Default is 1000.}

\item{degree}{Optional integer polynomial degree for the conditional mean
calibration model. If \code{NULL}, the degree is selected by AIC.}

\item{max_degree}{Maximum polynomial degree considered when \code{degree = NULL}.
Default is 5.}

\item{conditional}{Logical. If \code{TRUE}, use model-based residual resampling.
If \code{FALSE}, nonparametrically resample labeled rows before refitting the
calibration model.}

\item{statistic}{Optional function of the form \verb{function(X, y)} returning a
numeric vector. Defaults to OLS coefficients from \code{stats::lm.fit()}.}

\item{seed}{Optional integer random seed.}

\item{return_boot}{Logical. If \code{TRUE}, return bootstrap replicate matrices.}

\item{...}{Additional arguments accepted for compatibility with the \code{ipd()}
wrapper. They are currently unused.}
}
\value{
A list with the following components:
\describe{
\item{est}{Named numeric vector of bootstrap estimates computed on
generated unlabeled outcomes.}
\item{se}{Named numeric vector of bootstrap standard errors computed from
generated unlabeled outcomes.}
\item{labeled_est}{Named numeric vector of bootstrap estimates computed on
generated labeled outcomes.}
\item{labeled_se}{Named numeric vector of bootstrap standard errors
computed from generated labeled outcomes.}
\item{classical_est}{Named numeric vector of the downstream statistic
computed from the observed labeled data.}
\item{calibration_model}{List containing the fitted mean and scale
calibration objects and prediction functions.}
\item{degree}{Integer polynomial degree used in the mean calibration
model.}
\item{conditional}{Logical value indicating whether conditional resampling
was used.}
\item{nboot}{Integer number of bootstrap replicates.}
\item{boot_unlabeled}{Bootstrap replicate matrix for unlabeled outcomes
when \code{return_boot = TRUE}; otherwise \code{NULL}.}
\item{boot_labeled}{Bootstrap replicate matrix for labeled outcomes when
\code{return_boot = TRUE}; otherwise \code{NULL}.}
}
}
\description{
\code{efron_boot_ols()} implements a PPboot2-inspired bootstrap procedure for
continuous responses. The helper calibrates the relationship between the
observed labeled outcomes and their predictions, simulates bootstrap
outcomes for the unlabeled sample, and evaluates an OLS-style downstream
statistic on each bootstrap sample.
}
\details{
The calibration model estimates the conditional mean of \code{Y_l} given \code{f_l}
with a raw polynomial in \code{f_l}. When \code{degree = NULL}, the polynomial degree is
selected by AIC over degrees 1 through \code{max_degree}. A smooth conditional
scale estimate is then fit as a function of \code{f_l}, standardized residuals are
sampled with replacement, and bootstrap outcomes are generated for both the
unlabeled and labeled design matrices.
}
\examples{
set.seed(1)
n_l <- 80
n_u <- 120
x_l <- rnorm(n_l)
x_u <- rnorm(n_u)

X_l <- cbind("(Intercept)" = 1, X1 = x_l)
X_u <- cbind("(Intercept)" = 1, X1 = x_u)
f_l <- 0.5 + 1.2 * x_l + rnorm(n_l, sd = 0.5)
f_u <- 0.5 + 1.2 * x_u + rnorm(n_u, sd = 0.5)
Y_l <- 0.2 + 0.8 * x_l + 0.7 * f_l + rnorm(n_l)

fit <- efron_boot_ols(
  X_l = X_l, Y_l = Y_l, f_l = f_l,
  X_u = X_u, f_u = f_u,
  nboot = 25, seed = 123
)

fit$est
fit$se

}

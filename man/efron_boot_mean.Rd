% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppboot_mean.R
\name{efron_boot_mean}
\alias{efron_boot_mean}
\title{Efron bootstrap PPI for mean estimation}
\usage{
efron_boot_mean(
  X_l,
  Y_l,
  f_l,
  X_u,
  f_u,
  nboot = 1000,
  binary = NULL,
  seed = NULL,
  return_boot = FALSE,
  ...
)
}
\arguments{
\item{X_l}{Numeric design matrix for labeled observations. Accepted for
interface compatibility; not used for mean estimation.}

\item{Y_l}{Numeric vector or one-column matrix of observed labeled outcomes.}

\item{f_l}{Numeric vector or one-column matrix of predictions for labeled
observations.}

\item{X_u}{Numeric design matrix for unlabeled observations. Accepted for
interface compatibility; not used for mean estimation.}

\item{f_u}{Numeric vector or one-column matrix of predictions for unlabeled
observations.}

\item{nboot}{Integer number of bootstrap replicates. Default is 1000.}

\item{binary}{Optional logical value indicating whether to use the binary
PPboot helper. If \code{NULL}, the function infers this from \code{Y_l}, \code{f_l}, and
\code{f_u}.}

\item{seed}{Optional integer random seed.}

\item{return_boot}{Logical. If \code{TRUE}, return bootstrap replicate matrices
from the selected PPboot helper.}

\item{...}{Additional arguments passed to \code{efron_boot_logistic()} when
\code{binary = TRUE} or to \code{efron_boot_ols()} when \code{binary = FALSE}.}
}
\value{
A list with the same components returned by
\code{efron_boot_logistic()} or \code{efron_boot_ols()}. The \code{est}, \code{se},
\code{labeled_est}, \code{labeled_se}, and \code{classical_est} components are
length-one named numeric vectors representing the marginal mean.
}
\description{
\code{efron_boot_mean()} estimates a marginal mean using the Efron PPboot
bootstrap helpers. It dispatches to the binary PPboot1 helper when the
outcome is binary and the predictions are valid probabilities; otherwise it
dispatches to the continuous PPboot2-inspired helper.
}
\details{
The design matrix arguments are accepted for compatibility with the
matrix-based \code{ipd()} helper interface. Mean estimation uses an intercept-only
downstream statistic, so \code{X_l} and \code{X_u} are not otherwise used.

When \code{binary = NULL}, the function treats the problem as binary if all
non-missing labeled outcomes are in \verb{\{0, 1\}} and all labeled and unlabeled
predictions are strictly between 0 and 1.
}
\examples{
set.seed(1)
n_l <- 80
n_u <- 120
f_l <- rnorm(n_l, mean = 1)
f_u <- rnorm(n_u, mean = 1)
Y_l <- 0.5 + 0.8 * f_l + rnorm(n_l)

fit <- efron_boot_mean(
  X_l = matrix(1, n_l, 1), Y_l = Y_l, f_l = f_l,
  X_u = matrix(1, n_u, 1), f_u = f_u,
  nboot = 25, seed = 123
)

fit$est
fit$se

}
